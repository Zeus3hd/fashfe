import styled from "styled-components";

const Wrapper = styled.footer``;
const FooterRights = styled.p`
  font-size: 0.9rem;
  color: #833895;
  padding: 0 0.5rem;
`;

export { Wrapper, FooterRights };
